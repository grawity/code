#!/usr/bin/env perl
use warnings;
use feature 'say';

for my $dir (@INC) {
	open(my $fh, "<", "$dir/perllocal.pod") or next;
	while (<$fh>) {
		/^=head2 .+ L<(.+)\|.+>$/ and say $1;
	}
	close($fh);
}
