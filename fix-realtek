#!/usr/bin/env bash

for nic in /sys/class/net/eth*; do
	dev=$(readlink -f $nic/device)
	bus=$(readlink -f $dev/..)

	echo "removing $dev"
	echo 1 > $dev/remove

	echo "rescanning $bus"
	echo 1 > $bus/rescan
done
