#!/usr/bin/env python3
import dns.name
import sys
import hashlib

def name_to_wire(name):
    return dns.name.from_text(name).to_wire()

def nzf_hash(name):
    return hashlib.sha1(name_to_wire(name))

for name in sys.argv[1:]:
    print(nzf_hash(name).hexdigest())
