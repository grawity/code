#!/usr/bin/env perl
use warnings;
use feature 'say';

for my $dir (@INC) {
	next unless -f "$dir/perllocal.pod";
	open(my $fh, "<", "$dir/perllocal.pod");
	while (<$fh>) {
		if (/^=head2 .+ L<(.+)\|.+>$/) {
			say $1;
		}
	}
}
