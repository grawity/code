#!/usr/bin/env bash

if crontab -l 2>/dev/null | grep -Eqs code/tools/pull; then
	echo "Adjusting crontab"
	crontab -l | sed "/ID=pull/ s/pull/dotrc/" | crontab -
fi

echo "Reexecuting tools/dotrc"
exec ~/code/tools/dotrc
