#!/usr/bin/env bash
. lib.bash || exit
vh="[height<=720]"
vf="[ext=mp4]"
f="bestvideo${vh}${vf}+bestaudio[ext=m4a]/bestvideo${vh}+bestaudio"
do: youtube-dl --add-metadata --console-title --format "$f" "$@"
