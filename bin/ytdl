#!/usr/bin/env bash
. lib.bash || exit
n="${prefix}${prefix+ - }%(title)s [%(id)s].%(ext)s"
do: youtube-dl \
	--console-title \
	--add-metadata \
	--embed-subs \
	--sub-langs "all,-live_chat" \
	--output "$n" \
	"$@"
