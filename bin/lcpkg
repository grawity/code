#!/usr/bin/env bash
# lcpkg -- list a package's contents, in color
# (group: lspkg, lspkgs, whatowns, lcpkg, sysup)

case ${0##*/} in
	llpkg)
		opts="-lh" ;;
	*)
		opts="" ;;
esac

for pkg; do
	lspkg "$pkg" | xargs -d '\n' ls $opts -d --color=always 2>&1 | ${PAGER:-more}
done
