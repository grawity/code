#!/usr/bin/env bash

set -e

. "$(git --exec-path)/git-sh-setup"
require_work_tree

ref=$1 prefix=$2

git merge -s ours --no-commit "$ref"
git read-tree --prefix="${prefix%/}/" -u "$ref"
#git commit
echo "Subtree merged."
