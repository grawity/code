#!/bin/sh

h=${XDG_CACHE_HOME:-$HOME/.cache}/dmenu.history

p='run »'

r=$(dmenu-print-history "$h" | dmenu -l 7 -p "$p")

if [ "$r" ]; then
	echo "$(date +%s) $r" >> "$h"

	echo "$r" | ${SHELL:-/bin/sh} &
fi
