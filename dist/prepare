#!/usr/bin/env bash
set -e

BASEDIR="$HOME/code"

SYS="$HOSTTYPE-$OSTYPE"

OBJDIR="$BASEDIR/obj/arch.$SYS"
SYSDIR="$BASEDIR/obj/host.$HOSTNAME"

# ensure $OBJDIR and $SYSDIR

mkdir -p "$OBJDIR"

ln -nsf "bin.$SYS" "$SYSDIR"
