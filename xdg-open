#!/usr/bin/env perl
use Nullroute::Lib;

for my $orig (@ARGV) {
	my @xlat = ($orig);
	if ($orig !~ /^(git):/) {
		if (open(my $proc, "-|", "uri", $orig)) {
			@xlat = grep {chomp || 1} <$proc>;
			close($proc);
		}
	}
	for my $uri (@xlat) {
		forked { exec("/usr/bin/xdg-open", $uri) };
	}
}
