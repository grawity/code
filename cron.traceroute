#!/bin/sh
dir=~/Attic/Misc/$(date +%Y)/traceroute
mkdir -p "$dir"
for host; do
	log="$dir/trace-$host-$(date +%Y-%m).log"
	{
		echo "--- $host ($(date -R) @$(date +%s)) ---"
		traceroute "$host"
		#mtr -r -w -c 1 --show-ips "$host"
		echo "ok $?"
	} \
	|& tee -a "$log"
done
