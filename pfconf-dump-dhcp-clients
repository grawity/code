#!/usr/bin/env perl
use warnings;
use strict;
use Data::Dumper;
use XML::Simple qw(:strict);

my $conf = XMLin("-", KeyAttr => [], ForceArray => []);

#print Dumper($conf);

for my $iface (values %{$conf->{dhcpd}}) {
	for my $entry (@{$iface->{staticmap}}) {
		printf "%s │ %-15s │ %s\n",
			$entry->{mac},
			$entry->{ipaddr},
			$entry->{descr};
	}
}
