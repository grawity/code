#!/usr/bin/env bash

args=()
cmd=()
flag=0

for arg; do
	if (( flag == 1 )); then
		cmd+=("$arg")
	elif [[ $arg == "--" ]]; then
		flag=1
	else
		args+=("$arg")
	fi
done

for arg in "${args[@]}"; do
	"${cmd[@]}" "$arg"
done
