#!/usr/bin/env bash

. lib.bash || exit

set -e

if have pacman; then
	sudo: pacman -Syu "$@"
elif have apt-get; then
	sudo: apt-get update
	sudo: apt-get dist-upgrade -V "$@"
elif have yum; then
	sudo: yum update "$@"
else
	die "no known package manager found"
fi
