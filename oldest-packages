#!/bin/sh
echo "-- By install date --"
pacman -Qi | awk '/^Name/ {n=$3} /^Install Date/ {if ($4 ~ /^201[12]/) {print $4, n}}' | natsort
echo "-- By build date --"
pacman -Qi | awk '/^Name/ {n=$3} /^Build Date/ {if ($4 ~ /^201[12]/) {print $4, n}}' | natsort
