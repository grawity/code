#!/usr/bin/env bash
# lspkg -- list a package's contents

tool=$(osguess package-mgr) || exit

case $tool in
	dpkg)
		dpkg -L "$@";;
	pacman)
		pacman -Qql "$@";;
	rpm)
		rpm -ql "$@";;
	pkg-freebsd)
		pkg query %Fp "$@";;
	*)
		echo "$0: unknown distribution" >&2
		exit 2
esac
