#!/bin/sh
for host; do
	log=~/Attic/Misc/$(date +%Y)/trace-$host-$(date +%Y-%m).log
	{
		echo "--- $host ($(date -R) @$(date +%s)) ---"
		traceroute "$host"
		#mtr -r -w -c 1 --show-ips "$host"
		echo "ok $?"
	} \
	|& tee -a "$log"
done
