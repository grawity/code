#!/usr/bin/env perl
# lists modification time of all given files

if (!@ARGV) {
	warn "Usage: mtime <files>\n";
	exit 1;
}

if ($ARGV[0] eq "-q") {
	shift @ARGV;
	print for map {((stat $_)[9] // "?\t"), "\n"} @ARGV;
} else {
	print for map {((stat $_)[9] // "?\t"), "\t", $_, "\n"} @ARGV;
}
