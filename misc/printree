#!/usr/bin/env perl
use Data::Dumper;

my $tree = {};

sub put {
	my $branch = shift;
	my @path = split m|/|, shift;
	while (@path) {
		my $head = shift @path;
		$branch = ($branch->{$head} //= {});
	}
}

while (<STDIN>) {
	chomp; put($tree, $_);
}

print Dumper($tree);
