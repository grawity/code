#!/usr/bin/env bash
# newid -- generate a random PKCS#11 "object ID"

nbytes=${1:-8}
hexid=$(head -c "$nbytes" /dev/urandom | hex | sed "s/../:&/g; s/^://")

echo "newid is $hexid" >&2

echo "$hexid"
