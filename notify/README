USAGE

Server:

	./notify-receive.pl

	Optionally, specify the UDP port:

	./notify-receive.pl 31337

Client:

	0. mv notify-send.pl ~/.irssi/scripts/

	1. /script load notify-send

TODO

- Add an option to only listen on 127.0.0.1 or [::1] (security)
