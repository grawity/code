#!/usr/bin/env perl
use open qw(:std :encoding(UTF-8));

sub hilight {
	my ($c) = @_;
	$c = sprintf "<U+%04X>", ord($c);
	return "\e[1;37;41m$c\e[m";
}

while (<>) {
	s/[^\n\t\x20-\x7e]/hilight($&)/ge;
	print;
}
