#!/usr/bin/env bash
# git-up -- fast-forward the latest changes from upstream

if dir=$(git rev-parse --show-cdup); then
	cd "$dir" && pull -t "$@"
fi
# TODO: handle bare repos, both here and in 'pull'
