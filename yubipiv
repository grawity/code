#!/usr/bin/env bash

accdb_item="yubikey piv"

key_pin=$(ad get "$accdb_item" !pin)
key_puk=$(ad get "$accdb_item" !puk)
key_mgmt=$(ad get "$accdb_item" !management.psk)

yubico-piv-tool -k "$key_mgmt" "$@"
