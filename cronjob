#!/bin/sh
# wrapper for cronjobs, to set basic environment

export XDG_RUNTIME_DIR="/run/user/1000"
export SSH_AUTH_SOCK="$XDG_RUNTIME_DIR/ssh-agent"

if [ "$1" = "-c" ]; then
	set -- sh "$@"
fi

. ~/lib/dotfiles/environ && nice chronic "$@"
