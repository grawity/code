#!/usr/bin/env bash
# Pull from origin, return true if there were new commits

branch=master
ref="refs/heads/$branch"

old=$(git show-ref --hash "$ref")
git pull -q && {
	new=$(git show-ref --hash "$ref")
	[[ $old != $new ]]
}
