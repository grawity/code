#!/usr/bin/env perl

@run = ("openssl", "req", "-noout", "-text");

for (@ARGV) {
	if (/^-d$/ || /^--der$/) {
		push @run, ("-inform", "DER");
	} elsif (-f $_) {
		push @run, ("-in", $_);
	} else {
		push @run, $_;
	}
}

system @run;
