#!/bin/sh

PLAN9=${PLAN9:-/opt/plan9}
export PLAN9

case "$PATH" in
$PLAN9/bin:*)
	;;
*)
	PATH=$PLAN9/bin:$PATH
	export PATH
	;;
esac

if [ $# -gt 0 ]; then
	arg0=$1
	if [ -x "$PLAN9/bin/$1" ]; then
		set -- "$PLAN9/bin/$1" "${@:2}"
	fi
	exec -a "$arg0" "$@"
fi
