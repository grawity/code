#!/bin/bash

. lib.bash || exit

name=sha1sums
name=directory.sha

if [[ -s "$name" ]]; then
	die "'$name' already exists, not overwriting"
fi

find "${@:-.}" -type f \( -not -name "$name" \) -exec sha1sum -b {} + | tee -a "$name"
