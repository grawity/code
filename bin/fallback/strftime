#!/usr/bin/env perl
use warnings;
use POSIX;

if (!@ARGV) {
	warn "error: missing output format\n";
	warn "usage: $0 <format> [timestamp]\n";
	exit 1;
}

my $format = shift @ARGV;

print strftime($format, localtime($_))."\n" for @ARGV;
