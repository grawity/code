#!/usr/bin/env bash

. lib.bash || exit

cmd='whois'
arg=$1

if [[ ! $arg ]]; then
	die "missing query argument"
fi

if [[ $arg =~ ^[0-9]+$ ]]; then
	arg="AS${arg}"
fi

if [[ $cmd && $arg ]]; then
	url="http://dn42.netrik.de/de-fra1/ajax.php?cmd=${cmd}&arg=${arg}"
	curl -sS "$url"
fi
